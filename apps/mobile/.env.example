# Copy this file to .env (git-ignored) to override the API base URL used by
# the mobile app. Without a .env file, the app defaults to the deployed AWS
# Lambda (see apps/mobile/src/App.tsx).
#
# Point this at a local backend to develop/test against it instead:
API_BASE_URL=http://localhost:3000
