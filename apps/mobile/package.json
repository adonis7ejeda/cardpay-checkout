{
  "name": "@cardpay/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node -e \"console.log('Mobile TypeScript shell ready. Native runtime is deferred for this technical-test slice.')\"",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "dependencies": {
    "@cardpay/contracts": "workspace:*",
    "@cardpay/core": "workspace:*",
    "@reduxjs/toolkit": "^2.5.0"
  }
}
