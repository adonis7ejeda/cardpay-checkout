{
  "name": "@cardpay/core",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "dependencies": {
    "@cardpay/contracts": "workspace:*"
  }
}
